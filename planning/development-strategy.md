# HYF-Digitalent

## 1. Logo and navigation

## This is our development strategy for website HYF

## Table of contents 🗒️

- [Home page](#home-page)
- [Program page](#the-program-page)
- [Volunteer](#volunteer-page)
- [Digitalents](#digitalent-page)
- [About](#about-page)

## `Home` page

---

## Logo and navigation

### As a user I want to see logo and navigation of the page

- This user story is developed on branch `logoNavHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `nav`
- add a `div` parent
- add `img`
- add `ul` and inside ul `li`
- add `a` inside `li`
- add `button`

### CSS

- add class `logo` to `img`
- make `display`flex and add `padding`
- add class `nav-link` to `a`
- add `color` and `letter-spacing`
- add class `nav-button` to `button`

## Title and background

### As a user I want to see the title and background of the page

- This user story is developed on branch `titleBgHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `header` tag
- add `div` tag
- add `h1` for the page title
- add `span` tag

### CSS

- add class`title` to the `header`
- make the position `absolute` and add `margin-top`, style `font`
- add class`background` to the `div`
- add `display` and `background` and `width` and `height`

## Sections

### As a user I want to see info sections

- This user story is developed on branch `sectionsHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `main` tag
- add `section` tag
- add `h2` tag
- add `p` tag

### CSS

- make display `flex` in `main`
- add class `volunteer` to `section`
- add `border-top`, `color`,`font-size`,
- add class `about`
- make position `absolute` and add `padding-right`

## Our impact

### As a user I want to see achievement stats

- This user story is developed on branch `statisticsHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `div`
- add `h2`
- add `p`
- add `div`
- add `h2`
- add `p`

### CSS

- add class `statistic` to `div`
- make `display` flex and add `padding`
- add `width` and `height`
- add class `students`, `intership-job`, `alumni` to 3 `div`
- add class `stats1`, `stats2`, `stats3` to 3 `div`
- add `width` and `height`

## Core value

### As a user I want to see description of the page

- This user story is developed on branch `coreValueHome`.
- This branch is merged to `master` branch after completion.

### HTML

- add `div`
- add `h2`
- add `div`
- add `div`
- add `img`
- add `h4`
- add `p`

### CSS

- add class `core-value` to `div`
- add `display`,`width`,`background-color`,`margin-top`, `box shadow`
- add class `our-values` to `h2`
- add class `grid` to `div`
- add classes `community`, `diversity`, `commitment`, `open` to 4 `div`
- add styles `width`, `display`, `line-height`
- make the font-size `1.8rem`

## Partners

### As a user I want to see more links the page

- This user story is developed on branch `partnersHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `div`
- add `h2`
- add `div`
- add `div`

### CSS

- add class `partners` to `div`
- add class `partners` to `div`
- add class `logo-partners` to `div`
- add class `img-partners` to 16 `div`
- add alt to all `img`

## Contact & location

### As a user I want to see more links the page

- This user story is developed on branch `contactHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `div`
- add `div`
- add `h3`
- add `p`
- add `form`
- add `div`
- add `label`
- add `input`
- add `button`

### CSS

- add class `contact-location` to `div`
- add `display`,`width`,`background-color`,`border-radius`
- add class `contact-title` to `div`
- add `display`,`width`,`background-color`,`border-radius`, `padding`
- add class `contact-us` to `h3`
- add class `contact-description` to `p`
- add `font-size`,`margin-bottom`
- add class `form-btn` to `button`
- add `height`,`width`,`background`,`border-radius`, `border-style`

## Footer

### As a user I want to see footer to contact the website owner

- This user story is developed on branch `footerHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `footer` tag
- add `div` tag
- add `nav` tag
- add `address` tag
- add `ul` tag
- add `a` tag
- add `li` tag in `a`

### CSS

- add class `footer` to `footer` tag and `hyf-info` to `div`
- add `position`, `width`, `margin-top`
- add class `nav-footer` to nav and class `address` to `address`
- add `padding`, `width`, `display`, `line-height`
- add class `social-media` in `ul` tag and class `social-icon` in `li` which is
  inside the `a` tag
- add `display`, `width`

## `The program` page

---

## Logo and navigation bar

- As a user I can see the title and navigation bar of the website.
  - The website needs title and navigation bar at the top of the page for .
  - This feature is developed on a branch `logNavProgram`

### HTML

- add `nav`
- add a `div` parent
- add `img`
- add `ul`and inside ul `li`
- add `a` inside `li`
- add `button`
- add `header` tag
- add `div` tag
- add `h1` for the page title
- add `span` tag

### CSS

- add class `logo` to `img`
- make `display`flex and add `padding`
- add class `nav-link` to `a`
- add `color` and `letter-spacing`
- add class `nav-button` to `button`
- add class`title` to the `header`
- make the position `absolute` and add `margin-top`, style `font`
- add class`background` to the `div`
- add `display` and `background` and `width` and `height`

---

## Description, image, button `APPLY NOW`

- As a user I can see the description of the website, image and button
  `APPLY NOW`.
  - There is description of the website, image and button `APPLY NOW`.
  - This feature is developed on a branch `descriptionImageButtonProgram`

### HTML

- add `section` tag
- add `div` tag
- add `h1` tag
- add `p` tag
- add `a` tag

### CSS

- add `introduction` class to `section`
- add `info-introduction` class to `div`
- add `title-introduction` class to `h1`
- add `links-introduction` class to `p`
- add `apply-introduction` to `a`

---

## Information about who can apply

- As a user I can see information about who can apply on the program.
  - There is information about who can apply on the program.
  - This feature is developed on a branch `infoWhoApplyProgram`

### HTML

- add `section` tag
- add `div` tag
- add `h2` tag
- add `p` tag
- add `ul` tag

### CSS

- add `info`class to`section`
- add `apply-info`class to`div`
- add `who-apply-info` class to `h2`
- add `description-info` class to `p`
- add `requirements-info` class to `ul`

---

## Buttons and learning path

- As a user I can see the button `DISCOVER THE FULL CURRICULUM HERE`, the button
  `BECOME A COACH` and images of learning path.
  - There is the button `DISCOVER THE FULL CURRICULUM HERE`, the button
    `BECOME A COACH` and images of learning path.
  - This feature is developed on a branch `buttonsProgram`

### HTML

- add `section` tag
- add `div` tag
- add `h2` tag to `div`
- add 6 `img` tag to `div`

### CSS

- add `program-info` class to `section`
- add `buttons-program-info` class to `div`
- add `title-program-info` class to `h2`
- add `learning-path__program-info` to `img`

---

## Final project

- As a user I can see information about final project, image and the link
  `DISCOVER MORE`.
  - There is information about final project, image and the link
    `DISCOVER MORE`.
    - This feature is developed on a branch `finalProjectProgram`

### HTML

- add `section` tag
- add `div` tag
- add `h2` tag
- add `h4` tag
- add `p` tag
- add `a` tag

### CSS

- add `final-project` class to `section`
- add `description-final-project` class to `div`
- add `title-final-project` class to `h4`
- add `name-final-project` class to `h2`
- add `info-final-project` class to `p`
- add `link-final-project` class to `a`
- `background` to `div`

---

## Mentorship Info

- As a user I can see information about mentorship program, image and the button
  `BECOME A MENTOR`.
  - This feature is developed on a branch `mentorshipInfoProgram`

### HTML

- add `section` tag
- add `div` tag
- add `h4` tag
- add `p` tag
- add `a` tag

### CSS

- add `mentorship` class to `section`
- add `info-mentorship` class to `div`
- add `title-mentorship` class to `h4`
- add `description-mentorship` class to `p`
- add `link-mentorship` class to `a`
- `background` to `div`

---

## Career Skills

- As a user I can see information about career skills sessions, image.
  - There is information about career skills sessions, image.
  - This feature is developed on a branch `careerSkillsProgram`

### HTML

- add `section` tag
- add `div` tag
- add `h4` tag
- add `p` tag

### CSS

- add `career-skills` class to `section`
- add `info-career-skills` class to `div`
- add `title-career-skills` class to `h4`
- add `description-career-skills` class to `p`
- `background` to `div`

## Footer program

### As a user I can see the footer with address and social media links

- There is the footer with address and social media links.
- This feature is developed on a branch `footerProgram`.

### HTML

- add `footer` tag
- add `div` tag
- add `nav` tag
- add `address` tag
- add `ul` tag
- add `a` tag
- add `li` tag in `a`

### CSS

- add class `footer` to `footer` tag and `hyf-info` to `div`
- add `position`, `width`, `margin-top`
- add class `nav-footer` to nav and class `address` to `address`
- add `padding`, `width`, `display`, `line-height`
- add class `social-media` in `ul` tag and class `social-icon` in `li` which is
  inside the `a` tag
- add## `The program` page

## `Volunteer` page

### As a user I want to see logo and navigation of the page

- This user story is developed on branch `logoNavDigitalent`.
- This branch is merged to `main` branch after completion.

### html

### Title and description

### As a user I want to see a title and a description

### about volunteering on the page so that I can read about it

- _There is a title and a description on the page_

### HTML

- add `section`
- add `div`
- add `h2`
- add `p1`

### CSS

- add class `mainText` to `section`
- add class `mainText-container
- add class `mainText-container__title` to `h2`
- add class `,ainText-container__description` to `p`

## Text and images

### As a user I want to see title, short description

### and small images about reasons of volunteering on the page

### so that I can understand the reasons

- _There is a title, short description and small image in the page_

### HTML

- add `section`
- add `div`
- add `h3`
- add `div`
- add `img`
- add `p`
- add `div`
- add `img`
- add `p`
- add `div`
- add `img`
- add `p`
- add `div`
- add `img`
- add `p`

### CSS

- add class `textImg` to `section`
- add class `textImg-containerH3` to `div`
- add class `textImg-containerH3__title` to `h3`
- add classes `textImg-container1`, `container2`, `container3`, `container4` to
  all `div`
- add class `textImg-img` to all `img`
- add class `textImg-p` to all `p`

## Image and description coach guidebook

### As a user I want to see coach guidebooks and related image

### on the page so that I can discover it

- _There is a short quote of coach guidebook and related image in page_

### HTML

- add `section`
- add `div`
- add `img`
- add `div`
- add `h4`
- add `p`
- add `a`

### CSS

- add class `guidebook` to `section`
- add class `guidebook-container1` to 1-st `div`
- add class `guidebook-img` to `img`
- add class `guidebook-h4` to `h4`
- add class `guidebook-p` to `p`
- add class `guidebook-link` to `a`

## `Digitalent` page

- add `nav`
- add a `div` parent
- add `img`
- add `ul` and inside `li`
- add `a` inside `li`
- add `button`

### css

- add class `logo` to `img`
- make `display` flex and add `padding`
- add class `nav-link` to `a`
- add `color` and `letter-spacing`
- add class `nav-button` to `button`

## 2. Description

### As a user I want to see the title and

### description of the page

- This user story is developed on branch `descriptionDigitalent`.
- This branch is merged to `main` branch after completion.

### html

- add `main`
- add `section`
- add `div`
- add `h5`
- add `p`

### css

- add class `main-title` to `main`
- add class `digitalent-head` to `section`
- add class `description` to `div`

## 3. Programme

### As a user I want to see the details about offers of the programme

- This user story is developed on branch `programmeDigitalent`.
- This branch is merged to `main` branch after completion.

### html

- add `section`
- add `div`
- add `h2`
- add `h3`
- add `p`

### css

- add class `digitalent-sub-head` to `section`
- add class `container` to `div`

## 4. Information

### As a user, I want to see a section titled "Who

### are we looking for?" and I want ### to see a

### button that allows me to easily apply for the

### digitalents program

- This user story is developed on branch `infoDigitalent`.
- This branch is merged to `main` branch after completion.

### html

- add `section`
- add `div`
- add `img`
- add `h4`
- add `p`

### css

- add class `digitalent-info` to `section`
- add class `container-info` to `div`

## 5. Footer

### As a user I want to see footer to contact the website owner

- This user story is developed on branch `footerDigitalent`.
- This branch is merged to `main` branch after completion.

### html

- add `footer` tag
- add `div` tag
- add `nav` tag
- add `address` tag
- add `ul` tag
- add `a` tag
- add `li` tag in `a`

### css

## Logo and navigation digitalent

### As a user I want to see logo and navigation of the page

- This user story is developed on branch `logoNavDigitalent`.
- This branch is merged to `main` branch after completion.

### HTML

- add `nav`
- add a `div` parent
- add `img`
- add `ul` and inside `li`
- add `a` inside `li`
- add `button`

### CSS

- add class `logo` to `img`
- make `display` flex and add `padding`
- add class `nav-link` to `a`
- add `color` and `letter-spacing`
- add class `nav-button` to `button`

## Description

### As a user I want to see the title and

### description of the page

- This user story is developed on branch `descriptionDigitalent`.
- This branch is merged to `main` branch after completion.

### HTML

- add `main`
- add `section`
- add `div`
- add `h5`
- add `p`

### CSS

- add class `main-title` to `main`
- add class `digitalent-head` to `section`
- add class `description` to `div`

## Programme

### As a user I want to see the details about offers of the programme

- This user story is developed on branch `programmeDigitalent`.
- This branch is merged to `main` branch after completion.

### HTML

- add `section`
- add `div`
- add `h2`
- add `h3`
- add `p`

### CSS

- add class `digitalent-sub-head` to `section`
- add class `container` to `div`

## Information

- As a user, I want to see a section titled "Who

are we looking for?" and I want ### to see a

button that allows me to easily apply for the

### digitalents program

- This user story is developed on branch `infoDigitalent`.
- This branch is merged to `main` branch after completion.

### HTML

- add `section`
- add `div`
- add `img`
- add `h4`
- add `p`

### CSS

- add class `digitalent-info` to `section`
- add class `container-info` to `div`

## Footer digitalents

### As a user I want to see footer to contact the website owner

- This user story is developed on branch `footerDigitalent`.
- This branch is merged to `main` branch after completion.

### HTML

- add `footer` tag
- add `div` tag
- add `nav` tag
- add `address` tag
- add `ul` tag
- add `a` tag
- add `li` tag in `a`

### CSS

- add class `footer` to `footer` tag and `hyf-info` to `div`
- add `position`, `width`, `margin-top`
- add class `nav-footer` to nav and class `address` to `address`
- add `padding`, `width`, `display`, `line-height`
- add class `social-media` in `ul` tag and class `social-icon` in `li` which is
  inside the `a` tag
- add `display`, `width`

## `About` page

## Logo and navigation about

### As a user I want to see logo and navigation of the page

- This user story is developed on branch `logoNavHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `nav`
- add a `div` parent
- add `img`
- add `ul`and inside ul `li`
- add `a` inside `li`
- add `button`

### CSS

- add class `logo` to `img`
- make `display`flex and add `padding`
- add class `nav-link` to `a`
- add `color` and `letter-spacing`
- add class `nav-button` to `button`

## Title

### As a user I want to see the title of the page

- This user story is developed on branch `titleBgHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `h1` for the page title

### CSS

- add class`title` to the `header`

## Text & Image

As a user I want to see image with short description related to the website.

### HTML

- add `div`
- add `h3`
- add `img`

### CSS

- add class `textImg` to `section`
- add class `textImg-containerH3` to `div`

## Mission

As a user I want to see image with description for the mission to the website.

### HTML

- add `div`
- add `h2`
- add `p`
- add `img`

### CSS

- add padding & display flex
- add box shadow

## Vision

As a user I want to see image with description for the vision to the website.

### HTML

- add `div`
- add `h2`
- add `p`
- add `img`

### CSS

- add padding & display flex
- add box shadow

## History

As a user I want to see history of the website with a photo for the team.

### HTML

- add `div`
- add `h2`
- add `p`
- add `img`

### CSS

- add background
- add padding
- box shadow

## Team

As a user I want to see photo, position & social media links for the team member
of the website

### HTML

- add `div`
- add `h1`

### CSS

- add padding & display flex
- box shadow
- add `img`

## Footer about

### As a user I want to see footer to contact the website owner

- This user story is developed on branch `footerHome`.
- This branch is merged to `main` branch after completion.

### HTML

- add `footer` tag
- add `div` tag
- add `nav` tag
- add `address` tag
- add `ul` tag
- add `a` tag
- add `li` tag in `a`

### CSS

- add class `footer` to `footer` tag and `hyf-info` to `div`
- add `position`, `width`, `margin-top`
- add class `nav-footer` to nav and class `address` to `address`
- add `padding`, `width`, `display`, `line-height`
- add class `social-media` in `ul` tag and class `social-icon` in `li` which is
  inside the `a` tag
- add `display`, `width`
